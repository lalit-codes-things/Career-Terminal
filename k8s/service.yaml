apiVersion: v1
kind: Service
metadata:
  name: career-terminal-api
  namespace: career-terminal
  labels:
    app: career-terminal-api
spec:
  type: ClusterIP
  selector:
    app: career-terminal-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
