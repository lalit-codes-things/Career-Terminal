# Kubernetes RBAC — Workload Identity Secret Access (Epic 0.7, Phase 2-3)
#
# This manifest implements least-privilege secret access per workload.
# Each ServiceAccount may only read the Secrets it requires.
#
# Architecture:
#   ServiceAccount (per workload)
#     └── RoleBinding → Role → Secret (named resource grants)
#
# In production with an External Secrets Operator or Vault Agent Injector,
# these ServiceAccount identities are also presented to the external secret
# manager to authenticate workloads without a static master credential.
#
# Application:
#   kubectl apply -f k8s/rbac.yaml -n career-terminal
#
# Secret access matrix (Phase 31):
#   career-terminal-api:            database, redis, jwt, encryption, internal-api-key, google-oauth
#   career-terminal-gmail-worker:   database, redis, encryption, google-oauth
#   career-terminal-resume-worker:  database, redis, storage-credentials
#   career-terminal-tracking-worker: database, redis, encryption

---
# ── Namespace ──────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Namespace
metadata:
  name: career-terminal
  labels:
    app.kubernetes.io/name: career-terminal

---
# ── ServiceAccounts ────────────────────────────────────────────────────────────

apiVersion: v1
kind: ServiceAccount
metadata:
  name: career-terminal-api
  namespace: career-terminal
  labels:
    app.kubernetes.io/component: api
    app.kubernetes.io/name: career-terminal
  annotations:
    # Vault k8s auth: vault will bind this SA to the career-terminal-api policy
    vault.hashicorp.com/role: "career-terminal-api"
    # AWS IRSA (if on EKS): replace with actual role ARN
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT:role/career-terminal-api
    # GCP Workload Identity (if on GKE): replace with actual service account
    # iam.gke.io/gcp-service-account: career-terminal-api@PROJECT.iam.gserviceaccount.com

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: career-terminal-gmail-worker
  namespace: career-terminal
  labels:
    app.kubernetes.io/component: gmail-worker
    app.kubernetes.io/name: career-terminal
  annotations:
    vault.hashicorp.com/role: "career-terminal-gmail-worker"

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: career-terminal-resume-worker
  namespace: career-terminal
  labels:
    app.kubernetes.io/component: resume-worker
    app.kubernetes.io/name: career-terminal
  annotations:
    vault.hashicorp.com/role: "career-terminal-resume-worker"

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: career-terminal-tracking-worker
  namespace: career-terminal
  labels:
    app.kubernetes.io/component: tracking-worker
    app.kubernetes.io/name: career-terminal
  annotations:
    vault.hashicorp.com/role: "career-terminal-tracking-worker"

---
# ── Roles — named secret grants (least privilege) ─────────────────────────────

# API: database, redis, jwt, encryption, internal-api-key, google-oauth
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: career-terminal-api-secrets
  namespace: career-terminal
  labels:
    app.kubernetes.io/component: api
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames:
      - career-terminal-secrets      # main secret (database, jwt, encryption, google, internal-api)
      - career-terminal-redis-secret # redis credentials
    verbs: ["get"]

---
# Gmail worker: database, redis, encryption, google-oauth
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: career-terminal-gmail-worker-secrets
  namespace: career-terminal
  labels:
    app.kubernetes.io/component: gmail-worker
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames:
      - career-terminal-gmail-secret  # database-url, encryption-key, google-oauth
      - career-terminal-redis-secret
    verbs: ["get"]

---
# Resume worker: database, redis, S3 storage credentials
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: career-terminal-resume-worker-secrets
  namespace: career-terminal
  labels:
    app.kubernetes.io/component: resume-worker
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames:
      - career-terminal-resume-secret # database-url, aws credentials
      - career-terminal-redis-secret
    verbs: ["get"]

---
# Tracking worker: database, redis, encryption
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: career-terminal-tracking-worker-secrets
  namespace: career-terminal
  labels:
    app.kubernetes.io/component: tracking-worker
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames:
      - career-terminal-tracking-secret # database-url, encryption-key
      - career-terminal-redis-secret
    verbs: ["get"]

---
# ── RoleBindings ──────────────────────────────────────────────────────────────

apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: career-terminal-api-secrets-binding
  namespace: career-terminal
subjects:
  - kind: ServiceAccount
    name: career-terminal-api
    namespace: career-terminal
roleRef:
  kind: Role
  name: career-terminal-api-secrets
  apiGroup: rbac.authorization.k8s.io

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: career-terminal-gmail-worker-secrets-binding
  namespace: career-terminal
subjects:
  - kind: ServiceAccount
    name: career-terminal-gmail-worker
    namespace: career-terminal
roleRef:
  kind: Role
  name: career-terminal-gmail-worker-secrets
  apiGroup: rbac.authorization.k8s.io

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: career-terminal-resume-worker-secrets-binding
  namespace: career-terminal
subjects:
  - kind: ServiceAccount
    name: career-terminal-resume-worker
    namespace: career-terminal
roleRef:
  kind: Role
  name: career-terminal-resume-worker-secrets
  apiGroup: rbac.authorization.k8s.io

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: career-terminal-tracking-worker-secrets-binding
  namespace: career-terminal
subjects:
  - kind: ServiceAccount
    name: career-terminal-tracking-worker
    namespace: career-terminal
roleRef:
  kind: Role
  name: career-terminal-tracking-worker-secrets
  apiGroup: rbac.authorization.k8s.io
