apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: career-terminal-worker
  namespace: career-terminal
  labels:
    app: career-terminal-worker
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: career-terminal-worker
