apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: career-terminal-api-pdb
  namespace: career-terminal
  labels:
    app: career-terminal-api
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: career-terminal-api
