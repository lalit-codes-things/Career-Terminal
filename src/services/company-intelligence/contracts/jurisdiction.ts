/**
 * Jurisdiction contract.
 */

/** ISO 3166-1 alpha-2 country code, e.g. 'US', 'GB', 'IN'. */
export type CountryCode = string;

/**
 * Jurisdiction code: a country alpha-2 optionally qualified by an ISO 3166-2
 * subdivision, e.g. 'GB', 'US-DE', 'IN-MH'.
 */
export type JurisdictionCode = string;
